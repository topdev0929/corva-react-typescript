export type GradientFillStop = {
  /**
   * Position in percents. Can be fractional.
   */
  pos: number;
  /**
   * Color for the stop. Named colors should also work but please avoid them.
   */
  color: string;
};
