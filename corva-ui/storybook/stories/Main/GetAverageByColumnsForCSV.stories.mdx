<Meta title="Utils/Main/getAverageByColumnsForCSV" />

# Main Utils

Common utils.

## getAverageByColumnsForCSV

Gets average by each column in CSV. Returns Array – first element is headers, second element is averages.

### Example

```javascript
import utils from '~/utils';

utils.getAverageByColumnsForCSV(someCSV);
```

#### Parameters

<div class="param">
  <div class="code-block small">
    <i>csv: CSV</i>
  </div>
  <div class="chip">Required</div>
</div>
